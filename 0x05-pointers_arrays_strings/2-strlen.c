#include "main.h"
#include <string.h>

/**
 * _strlen - entry point for string lenngth
 * @s: string to which length is calculated
 * Return: returns the length of string s
*/
int _strlen(char *s)
{
	return (strlen(s));
}
